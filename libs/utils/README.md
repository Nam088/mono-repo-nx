# utils

This library was generated with [Nx](https://nx.dev).

Reserved folders under `src/lib/` (each has `.gitkeep` until code lands): `constants/`, `types/`, `helpers/`, `errors/`, `formatters/`, `predicates/`. Export new modules from `src/index.ts` when you add `.ts` files.

## Building

Run `nx build utils` to build the library.

## Running unit tests

Run `nx test utils` to execute the unit tests via [Vitest](https://vitest.dev/).

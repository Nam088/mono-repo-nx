# infra-postgresql

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build infra-postgresql` to build the library.

## Running unit tests

Run `nx test infra-postgresql` to execute the unit tests via [Vitest](https://vitest.dev/).

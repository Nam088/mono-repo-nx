# infra-redis

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build infra-redis` to build the library.

## Running unit tests

Run `nx test infra-redis` to execute the unit tests via [Vitest](https://vitest.dev/).
